//mathobject methods

// var x={
//     a:10,
//     "b":20,
//     c:()=>{
//         console.log(3-0);
//     }
// }
// console.log(x.a);
// console.log(x["b"]-x["b"]);
// x.c();

var a = 10;
var b = 20;

// console.log(b - a); // 10 result
// console.log(a - b); // -10 rseult
// //(1)  Math.
//  console.log(Math.abs(a-b)); // abs means absolute. it gives 10 difference between a-b or b-a
                           // it works both ways without negative value
                            //it gives the absolute value or postiove value

//(2)Math.
// console.log(Math.sqrt(16));  // it return the sqare root of a number of a every number

//(3) Math.
//console.log(Math.cbrt(729));    // it return the cube root of a number of a every number

//(4) Math.
// console.log(Math.pow(5,2));   // it returns the power of a number
                     // first parameter is base 2nd parameter is exponential
//     or
//console.log(5**2);   

// (5)  Math.

//console.log(Math.sign(a));
// it gives the 1 for all positive values or -1 for all negative value

// (6)Math.

// console.log(Math.PI);  // it gives the pi value

// (7) Math.random 
// console.log(Math.random()*20); // every time try to print it gives the different numbers or random numbers b/w multiplied value
// console.log(Math.random()); // every time try to print it gives the different numbers or random numbers b/w 0-1
// console.log(Math.random(7));  // prameter value will not be taken
//(8) Math.max

//var a=29;
//var b=30;
//console.log(Math.max(a,b)); // it gives the max value of given parameters or numbers inside of it

//(9)Math.min
//console.log(Math.min(a,b));  // it gives the min value of given parameters or numbers inside of it

//(10)  Math.round

//console.log(Math.round(14.0) );// it gives round figure value 0-4 it gves the previous integer 5-9 gives the next integer

// function round(num) 
// {
//     const dec=num%1;
//     if(dec < 0.5)
//     {
//         num=num-dec;
//     }
//     else
//     {
//         num=num-dec +1;
//         //   or
//         // num=num+(1-dec)
//     }
//     console.log(num);
// }
// round(12.5);
// round(12.4)

// (11) Math.floor()

// console.log(Math.floor(24.9));  // it always rounded off the previous integer only

// // (11) Math.ciel()

// console.log(Math.ceil(24.9));  // it always rounded off the next integer only

// (13)
//console.log(Math.trunc(12.6)); // it gives the same answer as the floor but here from decimal it removes the decimal value
                               // but Math.floor round figure and gives previous value only
//                (or)
//console.log(parseInt(12.5));

let value=["red","white","gree"]






